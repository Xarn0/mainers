$(document).ready(()=>{
   $('.slider').slick({
      // dots:true,
      slidesToShow:1,
      arrows:true,
      adaptiveHeight:true,
      // slidesToScroll:1,
      // easing:'ease',
      infinite:false,
      draggable:false,
     swipe:false
   })
})